#!/bin/bash

NOTES_DIR="$HOME/notes"

file=$(
    find "$NOTES_DIR" -type f -not -path "*/.git/*" |
    sed "s|$NOTES_DIR/||" |
    fzf --preview "cat $NOTES_DIR/{}" --preview-window=right:70%:wrap
)

[[ -n $file ]] && vim "$NOTES_DIR/$file"
